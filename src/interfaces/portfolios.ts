export interface PortfoliosType {
  title: string;
  language: string[];
  image: string;
  url: string;
  disabled: boolean;
  description: string;
  contribution?: string;
}

export interface PortfolioSurfaceType {
  title: string;
  description: string;
  url: string;
}
